upstream backend {
    server backend:3000;
}

upstream frontend {
    server frontend:5173;
}

# HTTP -> редирект на HTTPS, плюс ACME challenge для продления сертификата
server {
    listen 80;
    server_name summer-camp-1.ru www.summer-camp-1.ru api.summer-camp-1.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    server_name summer-camp-1.ru www.summer-camp-1.ru api.summer-camp-1.ru;

    ssl_certificate /etc/letsencrypt/live/summer-camp-1.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/summer-camp-1.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        alias /app/uploads/;
        expires 30d;
    }

    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
